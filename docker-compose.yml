services:
  jekyll-dev:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ".:/srv/jekyll"
    command: >
      sh -c "bundle install && bundle exec jekyll serve --drafts --host 0.0.0.0 --port 4000"
    ports:
      - "4000:4000"
